# No need to modify any value in this file anymore!
# Copy .env.example to .env and adjust there instead.
#
# README.md now has all the information.

# --- OCI Configuration ---
# Your OCI home region, e.g., us-ashburn-1, eu-frankfurt-1
OCI_REGION=
# Your user OCID
OCI_USER_ID=
# Your tenancy OCID
OCI_TENANCY_ID=
# Fingerprint of your API key
OCI_KEY_FINGERPRINT=
# Absolute path to your OCI private key .pem file
OCI_PRIVATE_KEY_FILENAME=

# --- Instance Configuration ---
# The OCID of the subnet for the instance
OCI_SUBNET_ID=
# The OCID of the image to use. Required if not using OCI_BOOT_VOLUME_ID.
OCI_IMAGE_ID=
# The full contents of your SSH public key. MUST BE A SINGLE LINE.
OCI_SSH_PUBLIC_KEY="ssh-rsa AAAA..."

# --- Optional Instance Parameters ---
# The shape of the instance. Default: VM.Standard.A1.Flex
OCI_SHAPE="VM.Standard.A1.Flex"
# Number of OCPUs. Default: 4
OCI_OCPUS=4
# Memory in Gigabytes. Default: 24
OCI_MEMORY_IN_GBS=24
# Specific availability domain to try. Leave empty to try all ADs in the region.
# Can also be a JSON array of strings: '["AD_1_ID", "AD_2_ID"]'
OCI_AVAILABILITY_DOMAIN=
# OCID of an existing boot volume to use instead of an image.
OCI_BOOT_VOLUME_ID=
# Size of the boot volume in GB (e.g., 50, 100). Only used with OCI_IMAGE_ID.
OCI_BOOT_VOLUME_SIZE_IN_GBS=

# --- Application Behavior ---
# The maximum number of running instances of this shape before the script stops. Default: 1
OCI_MAX_INSTANCES=1
# The time in seconds to wait after receiving a "TooManyRequests" error. Default: 300
TOO_MANY_REQUESTS_TIME_WAIT=300

# --- Optional Telegram Notifications ---
# Create a bot with @BotFather to get an API key
TELEGRAM_BOT_API_KEY=
# Get your user ID from a bot like @userinfobot
TELEGRAM_USER_ID=